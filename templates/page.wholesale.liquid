{% comment %} 
<div class="section page-width-content color-{{ section.settings.color_scheme }}">
  <div
    class="
      spacing-style
      layout-panel-flex
      layout-panel-flex--column
      section-content-wrapper
      mobile-column
    "
    style="
      {% render 'layout-panel-style', settings: section.settings %}
      {% render 'spacing-style', settings: section.settings %}
    "
  >
    <h1 class="main-page-title page-title h0">{{ page.title | escape }}</h1>
  </div>
</div> {% endcomment %}
{% comment %} <div class="page-width page-width--narrow">
    {% comment %} <div>
        <h1 class="main-page-title page-title h0">{{ page.title | escape }}</h1>
    </div> {% endcomment %}
    <div class="rte">
    {{ page.content }}
    </div>
</div> {% endcomment %}



<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div class="section page-width-content color-{{ section.settings.color_scheme }}">
  <div
    class="
      spacing-style
      layout-panel-flex
      layout-panel-flex--column
      section-content-wrapper
      mobile-column
    "
    style="
      {% render 'layout-panel-style', settings: section.settings %}
      {% render 'spacing-style', settings: section.settings %}
    "
  >
    <h1 class="main-page-title page-title h0">{{ page.title | escape }}</h1>
  </div>
</div>

